/**
 * @deprecated Usar `@/features/skills/SkillsList` directamente.
 * Se conserva como alias para imports legacy sin StackChart/recharts.
 */
export { default } from '@/features/skills/SkillsList';
